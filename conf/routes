# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

# Home page
GET     /upvote/{namespace}/{key}		Application.upvote
GET     /downvote/{namespace}/{key}     Application.downvote
GET     /getvotes/{namespace}			Application.getvotes